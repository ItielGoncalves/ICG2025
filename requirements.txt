# Rotina do briefing diário em áudio
anthropic>=0.40          # OCR/visão dos prints (modelo de visão)
requests>=2.31           # ElevenLabs (TTS) + envio do áudio pela API do Telegram
pydub>=0.25              # manipulação de áudio (anexar silêncio, exportar)
imageio-ffmpeg>=0.5      # binário ffmpeg de fallback (recomenda-se ffmpeg do sistema)
gTTS>=2.5                # TTS de fallback (BRIEFING_TTS_ENGINE=gtts)
pytest>=8.0              # testes
